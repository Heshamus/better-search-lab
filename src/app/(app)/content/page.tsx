import { EmptyState } from "@/components/empty-state";

export default function ContentPage() {
  return (
    <EmptyState
      title="Content"
      description="Content opportunities will appear here once keyword gaps are detected."
    />
  );
}
