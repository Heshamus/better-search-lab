import "../globals.css";

// Temporary layout — Task 1 scaffolding only.
// Replaced by the real dashboard shell (nav + top bar) in a later task.
export default function AppLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
