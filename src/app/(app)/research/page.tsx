import { EmptyState } from "@/components/empty-state";

export default function ResearchPage() {
  return (
    <EmptyState
      title="Research"
      description="Start a search to explore keyword ideas, volume, and difficulty."
    />
  );
}
