import { EmptyState } from "@/components/empty-state";

export default function CompetitorsPage() {
  return (
    <EmptyState
      title="Competitors"
      description="No competitors added yet — add a competitor domain to see the keyword gap."
    />
  );
}
