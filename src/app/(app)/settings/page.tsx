import { EmptyState } from "@/components/empty-state";

export default function SettingsPage() {
  return (
    <EmptyState
      title="Settings"
      description="Project and account settings will appear here."
    />
  );
}
