import { EmptyState } from "@/components/empty-state";

export default function KeywordsPage() {
  return (
    <EmptyState
      title="Keywords"
      description="No keywords yet — research and add keywords to build your tracked set."
    />
  );
}
