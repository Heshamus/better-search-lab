import { EmptyState } from "@/components/empty-state";

export default function RankingsPage() {
  return (
    <EmptyState
      title="Rankings"
      description="No tracked keywords yet — add keywords to start tracking rank history."
    />
  );
}
